# PostgreSQL bağlantısı (Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/taxclara?schema=public"

# Auth.js (NextAuth v5) — `openssl rand -base64 32` və ya `npx auth secret` ilə yarat
AUTH_SECRET="your-generated-secret"
NEXTAUTH_URL="http://localhost:3000"
